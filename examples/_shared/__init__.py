"""Shared helpers for the worked examples.

Deliberately *not* part of the installable `interpost` package — this is code that
is local to the examples in Phases 1-2 and gets generalized into
`interpost.signals` / `interpost.interventions` in Phase 3.
"""
