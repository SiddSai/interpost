"""interpost — interpretability signals as first-class primitives for post-training.

This package is being built in phases (see ``docs/build-plan.md``). The public API
(``Signal``, ``Intervention``, the offline/online trainers) is extracted from a
working vertical slice in Phase 3; until then the subpackages are deliberately
empty namespaces that only fix the import layout.
"""

__version__ = "0.0.0"

__all__ = ["__version__"]
