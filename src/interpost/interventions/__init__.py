"""Intervention layer — implements modes 1-3 over a small set of hook points.

Planned: ``DataShaping`` (mode 1), ``LossShaping`` (mode 2), ``Preservation``
(mode 3), plus a roadmap-only ``circuit_redirection`` stub (mode 4). The
``Intervention`` ABC is extracted in Phase 3. Intentionally empty until then.
"""

__all__: list[str] = []
