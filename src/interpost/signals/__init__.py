"""Signal layer — wrap any interpretability tool as ``activations -> score``.

Planned implementations: ``LinearProbeSignal``, ``SAEFeatureSignal``,
``ActivationDirectionSignal``, ``AggregateSignal``. The ``Signal`` ABC is extracted
from the Phase 1-2 vertical slice in Phase 3. Intentionally empty until then.
"""

__all__: list[str] = []
