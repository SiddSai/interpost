"""Configuration objects for interpost trainers.

``OfflineConfig`` / ``OnlineConfig`` — thin wrappers over TRL's ``DPOConfig`` /
``GRPOConfig`` plus ``signal`` and ``interventions`` — are added in Phase 3 and
Phase 5 respectively. Nothing to configure yet.
"""

__all__: list[str] = []
